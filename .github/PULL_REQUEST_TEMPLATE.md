## 这个 PR 做了什么

<!-- 一两句话说明改动和原因。关联的 issue 写成 Fixes #123 -->

## 改动范围

- [ ] 后端（`backend/`）
- [ ] 前端（`frontend/`）
- [ ] 数据库迁移（只新增了 `V<新号>__*.sql`，没有修改旧文件）
- [ ] 脚本（`scripts/aitest.ps1` 与 `scripts/aitest.sh` 同步改了）
- [ ] 文档（`README.md` / `docs/`）

## 验证

- [ ] 本地 `.\scripts\aitest.ps1 verify` 全绿
- [ ] 新功能带了测试，或修 bug 先加了能复现的测试
- [ ] 改了接口的话，`docs/api-contract.md` 已同步

## 使用者可见的变化

<!-- 界面文字、配置项、命令行为有变化时写在这里；没有就写「无」 -->
